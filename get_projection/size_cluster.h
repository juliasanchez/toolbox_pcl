#ifndef SIZE_CLUSTER
#define SIZE_CLUSTER

#include <iostream>
#include <fstream>
#include <string>
#include <vector>

float size_cluster(Cluster clus);

#include "size_cluster.inl"

#endif // SIZE_CLUSTER
